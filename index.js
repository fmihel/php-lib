import dist from './dist/fmihel-browser-lib.min';

export const {
    DOM,
    DOMS,
    childDOM,
    $D,
    parentDOM,
    binds,
    flex,
    flexChild,
    propsToState,
    defaultProps,
    JX,
    ut,
} = dist;


export default {
    DOM,
    DOMS,
    childDOM,
    $D,
    parentDOM,
    binds,
    flex,
    flexChild,
    propsToState,
    defaultProps,
    JX,
    ut,
};
